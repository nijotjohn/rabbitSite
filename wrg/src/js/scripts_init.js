/* ========================================================================
 * main script file to initiate all other modules
 * ======================================================================== */

;(function(global, $){

  // initiations on window ready
  $(document).ready(function() {

    // accordion function init
    global.accordion.init();

  });

})(global, jQuery);
        